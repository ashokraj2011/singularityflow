{
  "schemaVersion": 1,
  "runtime": {
    "nodeMajor": 22,
    "runner": "ubuntu-latest",
    "platform": "linux",
    "architecture": "x64"
  },
  "protocol": {
    "warmupRuns": 1,
    "samples": 30,
    "network": "disabled",
    "modelCalls": "disabled"
  },
  "topology": {
    "trackedFiles": 500,
    "untrackedFiles": 4,
    "localBranches": 4,
    "stories": 1,
    "initiatives": 0,
    "specifications": 0,
    "knowledgeDocuments": 0,
    "localSubjectIndex": "absent"
  },
  "budgets": {
    "about": {
      "p50Ms": 150
    },
    "status": {
      "p50Ms": 150
    },
    "nextsteps": {
      "p50Ms": 150
    },
    "snapshot": {
      "p50Ms": 150,
      "p95Ms": 250
    },
    "snapshotUi": {
      "p50Ms": 400,
      "p95Ms": 650
    },
    "snapshotFull": {
      "p50Ms": 850,
      "p95Ms": 1100
    },
    "legacyReadsNote": [
      "Four read-only commands served by the legacy dispatcher rather than their own lazy module.",
      "Measured 148-218ms on the reference fixture, of which ~120ms is loading cli.mjs and its",
      "264-module static closure -- 58 to 82 percent of each, before the command does anything.",
      "They are budgeted well above the 150ms the fast four hold to, because that is what is true;",
      "the number to lower is the module load, and lowering it is what should lower these."
    ],
    "help": {
      "p50Ms": 400
    },
    "inbox": {
      "p50Ms": 450
    },
    "guide": {
      "p50Ms": 450
    },
    "logs": {
      "p50Ms": 350
    }
  },
  "scale": {
    "why": [
      "Every other tier measures one point. A point says snapshot --include repository took 102ms;",
      "it cannot say whether that is 102ms on any repository or 102ms on a five-hundred-file one.",
      "The reference fixture holds one Story and four branches, so a read path costing",
      "branches x Stories reads as constant there and as minutes on a real portfolio -- and the",
      "read model does spawn two Git processes per pair.",
      "Twenty times the files, forty times the Stories, three times the branches. Sized by what it",
      "has to show rather than by how large it can be: the subject-index cost follows branches x",
      "Stories and not the file count, so twenty thousand files cost forty more seconds per run and",
      "reported the same 14.21x. Ten thousand still grows the sliced snapshot's own time, which is",
      "the other thing this tier is watching.",
      "Enforced on the subprocess count rather than the clock: a count means the same on a fast",
      "runner and a loaded one, so it can carry a complexity claim that a time budget cannot."
    ],
    "topology": {
      "trackedFiles": 10000,
      "untrackedFiles": 4,
      "localBranches": 12,
      "stories": 40,
      "initiatives": 0,
      "specifications": 0,
      "knowledgeDocuments": 0,
      "localSubjectIndex": "absent"
    },
    "commands": [
      "status",
      "nextsteps",
      "snapshot",
      "snapshotUi",
      "snapshotFull"
    ],
    "subprocessGrowth": {
      "why": [
        "How many times more subprocesses a command may run on the larger repository.",
        "1.0 is the property worth having: the read answered the same question, so it should not",
        "have needed more processes to answer it. status, nextsteps and snapshot hold it exactly.",
        "snapshotFull first measured 966 subprocesses against 68 -- a 14.21x growth -- because",
        "buildRepositorySubjectIndexFromRefs spawned two Git processes per branch x Story pair:",
        "git show for each record, and git rev-parse for a ref head that does not vary per file.",
        "That was 960 of the 966, and the same shape as the 975 the connected tier's note describes",
        "on a real repository. Reading one tree per ref instead (git-ref-tree.mjs) took it to 108,",
        "a 1.59x growth, and the scale tier's own wall clock from 6,151ms to 1,855ms.",
        "What remains is linear in refs and not in Stories, which is correct: twelve branches",
        "genuinely hold twelve trees to read. 2.0 locks that in with room for one more per-ref call",
        "and refuses any return to per-Story work. The VS Code slice adds exactly one bounded read",
        "per additional branch (37 vs 29); 1.3 records that current shape without allowing file- or",
        "Story-proportional fan-out. Never raise either allowance."
      ],
      "status": 1.2,
      "nextsteps": 1.2,
      "snapshot": 1.2,
      "snapshotUi": 1.3,
      "snapshotFull": 2.0
    },
    "budgets": {
      "why": [
        "Deliberately absent. This tier is enforced on subprocess counts, which mean the same on",
        "every host; a wall-clock budget over a ten-thousand-file fixture on an unpinned runner",
        "would fail for load rather than for regression. Times are reported, not gated."
      ]
    }
  },
  "workingTree": {
    "why": [
      "The reference fixture has four untracked files, but a real IDE checkout also carries modified",
      "tracked files and staged renames while file watchers request repeated UI snapshots.",
      "This tier applies all three status shapes to the same deterministic fixture, then measures the",
      "exact snapshot slices requested by VS Code. It reports p50 and p95, while enforcing the",
      "machine-independent subprocess count: a dirty tree must not turn one refresh into per-path work."
    ],
    "command": "snapshotUi",
    "samples": 10,
    "topology": {
      "modifiedFiles": 64,
      "renamedFiles": 64,
      "untrackedFiles": 128
    },
    "subprocessGrowth": 1.2
  },
  "connected": {
    "why": [
      "The tier above has no remote, no ledger and one Story, so the entire cost centre that made",
      "snapshot --json take 12.6s on a real repository was invisible to it: 975 subprocesses, of",
      "which 6 were fetches worth 4.5s. It also runs with SINGULARITY_FLOW_NO_NETWORK=1, which",
      "blocks a fetch rather than performing one -- so even a fixture with a remote would have",
      "measured the fetches as free.",
      "This tier has a real (file://) remote, an enabled ledger with intents published on several",
      "branches, and a second capability repository. Network is left ENABLED, because the property",
      "under test is that a read path does not use it."
    ],
    "topology": {
      "repositories": 2,
      "remoteBranches": 4,
      "ledgerIntents": 24,
      "ledger": "enabled",
      "remote": "local-bare",
      "workBranches": 3,
      "remoteBranchesNote": "Three work branches carrying intents, plus the ledger's own state branch."
    },
    "budgets": {
      "snapshotFull": {
        "p50Ms": 2500,
        "p95Ms": 3500
      }
    },
    "networkCalls": {
      "snapshotFull": 0,
      "note": [
        "A budget cannot express this. Time budgets are met by a fast network and blown by a slow",
        "one, which is why the read path was allowed to fetch for as long as it did -- nothing was",
        "asserting that it must not. This counts network subprocesses via",
        "SINGULARITY_FLOW_SUBPROCESS_PROBE and is machine-independent: zero is zero on any runner."
      ]
    },
    "repositoryWrites": {
      "snapshotFull": 0,
      "note": [
        "A read answers questions and must not change the repository. The ledger's read queries",
        "each built a temporary worktree to read a few JSON files, so a snapshot wrote an entry",
        "under .git/worktrees, took the index lock, and created a branch it then deleted. An",
        "interrupted read left that behind; two concurrent reads contended for the lock."
      ]
    }
  }
}
