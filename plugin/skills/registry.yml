version: 1

# Only these skills may be selected automatically from natural-language requests.
# Every other skill remains user-invocable through its /sflow-* command.
#
# The bar for this list is not "useful" — it is "cannot change governed state". A newcomer should be
# able to ask what is going on and be answered, without having to recall one of 97 exact slash names;
# they should never be able to cause an approval or a publication by phrasing a question a particular
# way. So every entry here reads: status, progress, the inbox, diagnostics, logs, help, served
# documentation, and the
# sandboxed walkthrough. Nothing that submits, approves, publishes, merges, or writes to the ledger
# belongs here, however convenient it would be.
#
# sflow-inbox is deliberately NOT here despite being the obvious candidate. It does not merely list
# pending approvals: it asks the reviewer a question and attaches a session to open a review. That is
# a step into a decision, and a decision is precisely what a phrasing of a question must not start.
# "What is waiting on me?" is answered well enough by status and nextsteps.
automaticInvocationAllowlist:
  - sflow-docs
  - sflow-doctor
  - sflow-help
  - sflow-home
  - sflow-logs
  - sflow-nextsteps
  - sflow-progress
  - sflow-quickstart
  - sflow-receipt
  - sflow-recommend
  - sflow-status

classes:
  conversational:
    outputContract: guided-actions
    kernelModelPolicy: never
    warningTokens: 500
    maximumTokens: 800
    previewBytes: 16384
    hardMaximumBytes: 65536
  echo:
    outputContract: concise-relay
    kernelModelPolicy: never
    warningTokens: 500
    maximumTokens: 800
    previewBytes: 8192
    hardMaximumBytes: 65536
  interactive:
    outputContract: explicit-selection
    kernelModelPolicy: never
    warningTokens: 500
    maximumTokens: 800
    previewBytes: 16384
    hardMaximumBytes: 65536
  guided:
    outputContract: conversational-guidance
    kernelModelPolicy: never
    warningTokens: 500
    maximumTokens: 800
    previewBytes: 16384
    hardMaximumBytes: 65536
  review:
    outputContract: governed-review
    kernelModelPolicy: never
    warningTokens: 500
    maximumTokens: 800
    previewBytes: 16384
    hardMaximumBytes: 65536
  generative:
    outputContract: clarification-and-artifact
    kernelModelPolicy: never
    warningTokens: 500
    maximumTokens: 800
    previewBytes: 16384
    hardMaximumBytes: 65536
  mutation:
    outputContract: deterministic-mutation
    kernelModelPolicy: never
    warningTokens: 500
    maximumTokens: 800
    previewBytes: 16384
    hardMaximumBytes: 65536

# Every public skill is deliberately classified. All classes share the same body
# budget so catalog growth cannot hide behind per-skill exceptions.
skills:
  sflow-about: { class: echo }
  sflow-agent: { class: interactive }
  sflow-agents: { class: echo }
  sflow-admin: { class: conversational }
  sflow-approvals: { class: echo }
  sflow-approve: { class: review }
  sflow-assign: { class: mutation }
  sflow-cancel: { class: mutation }
  sflow-capabilities: { class: echo }
  sflow-capability-doctor: { class: echo }
  sflow-capability-map: { class: interactive }
  sflow-continue: { class: mutation }
  sflow-configuration: { class: mutation }
  sflow-constitution: { class: mutation }
  sflow-design: { class: generative }
  sflow-docs: { class: echo }
  sflow-specify: { class: echo }
  sflow-plan: { class: echo }
  sflow-converge: { class: echo }
  sflow-doctor: { class: echo }
  sflow-documents: { class: mutation }
  sflow-epic-complete: { class: review }
  sflow-epic-drift: { class: mutation }
  sflow-epic-journey: { class: echo }
  sflow-epic-merge-plan: { class: echo }
  sflow-epic-next: { class: echo }
  sflow-epic-planning: { class: generative }
  sflow-epic-publish: { class: review }
  sflow-epic-requirements: { class: generative }
  sflow-epic-resume: { class: interactive }
  sflow-epic-review: { class: review }
  sflow-epic-review-decision: { class: review }
  sflow-epic-sources: { class: mutation }
  sflow-epic-start: { class: interactive }
  sflow-epic-status: { class: echo }
  sflow-epic-stories: { class: echo }
  sflow-epic-story-draft: { class: generative }
  sflow-epic-sync: { class: mutation }
  sflow-factory-reset: { class: mutation }
  sflow-fault: { class: mutation }
  sflow-fresh-install: { class: mutation }
  sflow-local-reset: { class: mutation }
  sflow-reinstall: { class: mutation }
  sflow-finalize: { class: mutation }
  sflow-fix: { class: interactive }
  sflow-gate: { class: mutation }
  sflow-goal: { class: interactive }
  sflow-journal: { class: interactive }
  sflow-harness: { class: echo }
  sflow-help: { class: conversational }
  sflow-home: { class: guided }
  sflow-hook: { class: mutation }
  sflow-implement: { class: generative }
  sflow-inbox: { class: echo }
  sflow-init: { class: mutation }
  sflow-impact: { class: mutation }
  sflow-inspect: { class: conversational }
  sflow-initiative-approve: { class: review }
  sflow-initiative-checklist: { class: echo }
  sflow-initiative-documents: { class: echo }
  sflow-initiative-evidence: { class: mutation }
  sflow-initiative-materialize: { class: mutation }
  sflow-initiative-next: { class: echo }
  sflow-initiative-phase: { class: generative, kernelModelPolicy: conditional }
  sflow-initiative-start: { class: interactive }
  sflow-initiative-status: { class: echo }
  sflow-inputs: { class: echo }
  sflow-jira-assigned: { class: echo }
  sflow-jira-board: { class: echo }
  sflow-jira-doctor: { class: echo }
  sflow-jira-initiative: { class: interactive }
  sflow-jira-status: { class: echo }
  sflow-jira-story: { class: echo }
  sflow-jira-update: { class: mutation }
  sflow-jira-work: { class: echo }
  sflow-knowledge: { class: mutation }
  sflow-ledger: { class: mutation }
  sflow-logs: { class: echo }
  sflow-mcp: { class: mutation }
  sflow-next: { class: mutation, kernelModelPolicy: conditional }
  sflow-nextsteps: { class: conversational }
  sflow-phase: { class: generative }
  sflow-plugin: { class: mutation }
  sflow-pr: { class: mutation }
  sflow-progress: { class: echo }
  sflow-push: { class: mutation }
  sflow-prompt-log: { class: mutation }
  sflow-quickstart: { class: echo }
  sflow-recommend: { class: conversational }
  sflow-receipt: { class: echo }
  sflow-refresh-branch: { class: mutation }
  sflow-regression-investigate: { class: generative }
  sflow-recover: { class: mutation }
  sflow-reject: { class: review }
  sflow-release: { class: generative }
  sflow-report: { class: echo }
  sflow-requirements: { class: generative }
  sflow-reset-all: { class: mutation }
  sflow-resume: { class: interactive }
  sflow-return: { class: interactive }
  sflow-review: { class: review }
  sflow-run: { class: mutation, kernelModelPolicy: conditional }
  sflow-secrets: { class: mutation }
  sflow-session: { class: interactive }
  sflow-show: { class: echo }
  sflow-show-prompt: { class: echo }
  sflow-snapshot: { class: echo }
  sflow-spec: { class: mutation, kernelModelPolicy: conditional }
  sflow-stack: { class: mutation }
  sflow-start: { class: interactive }
  sflow-status: { class: conversational }
  sflow-story-branch: { class: interactive }
  sflow-story-checks: { class: mutation }
  sflow-story-fetch: { class: interactive }
  sflow-story-inbox: { class: echo }
  sflow-work-interval: { class: mutation }
  sflow-story-start: { class: interactive, kernelModelPolicy: conditional }
  sflow-submit: { class: review }
  sflow-telemetry: { class: echo }
  sflow-upload: { class: mutation }
  sflow-verify: { class: generative }
  sflow-visual: { class: mutation }
  sflow-watch: { class: echo }
  sflow-workflow-rules: { class: conversational, kernelModelPolicy: conditional }
  sflow-workflows: { class: mutation }
  sflow-workspace: { class: interactive, kernelModelPolicy: conditional }
  sflow-workspace-bootstrap: { class: interactive }
  sflow-workspace-impact: { class: generative, kernelModelPolicy: conditional }
  sflow-workspace-session: { class: interactive }
  sflow-workspaces: { class: echo }
  sflow-worldmodel: { class: generative, kernelModelPolicy: conditional }
