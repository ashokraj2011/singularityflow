# Your local profile

Set your display name and role so the extension can keep each screen focused. Roles affect guidance only; the workflow phase selects the governed agent.

[Configure profile](command:singularityFlow.configureProfile)
