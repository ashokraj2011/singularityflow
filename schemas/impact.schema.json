{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://singularity-flow.local/schemas/impact.schema.json",
  "title": "Singularity Flow impact studies",
  "type": "object",
  "additionalProperties": false,
  "required": ["version", "studies"],
  "$defs": {
    "id": { "type": "string", "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$" },
    "metricId": {
      "enum": [
        "flow-time-excluding-approval-wait-ms", "elapsed-ms", "approval-wait-ms",
        "rework-cycles", "rejections", "self-approvals", "sequence-overrides",
        "input-tokens", "output-tokens", "cached-input-tokens", "total-tokens",
        "cost-usd", "first-pass-approval-rate",
        "required-check-pass-rate", "conformance-gap-count", "escaped-defects"
      ]
    },
    "group": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "label", "assistanceMode"],
      "properties": {
        "id": { "$ref": "#/$defs/id" },
        "label": { "type": "string", "minLength": 1 },
        "assistanceMode": { "enum": ["baseline", "assisted", "governed-agent", "custom"] },
        "weight": { "type": "number", "exclusiveMinimum": 0 }
      }
    },
    "primaryMetric": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id"],
      "properties": {
        "id": { "$ref": "#/$defs/metricId" },
        "direction": { "enum": ["higher", "lower"] }
      }
    },
    "guardrail": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "maximumRegressionPercent"],
      "properties": {
        "id": { "$ref": "#/$defs/metricId" },
        "maximumRegressionPercent": { "type": "number", "minimum": 0 }
      }
    },
    "metricAuthority": {
      "type": "object",
      "additionalProperties": false,
      "required": ["authority"],
      "properties": {
        "authority": { "enum": ["kernel-only", "external-provider", "attested", "composite"] },
        "providers": { "type": "array", "uniqueItems": true, "items": { "type": "string", "minLength": 1 } },
        "reducer": { "const": "prefer-kernel" }
      }
    }
  },
  "properties": {
    "version": { "const": 1 },
    "automaticEnrollment": { "type": "boolean" },
    "metricAuthorities": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "flow-time-excluding-approval-wait-ms": { "$ref": "#/$defs/metricAuthority" },
        "elapsed-ms": { "$ref": "#/$defs/metricAuthority" },
        "approval-wait-ms": { "$ref": "#/$defs/metricAuthority" },
        "rework-cycles": { "$ref": "#/$defs/metricAuthority" },
        "rejections": { "$ref": "#/$defs/metricAuthority" },
        "self-approvals": { "$ref": "#/$defs/metricAuthority" },
        "sequence-overrides": { "$ref": "#/$defs/metricAuthority" },
        "input-tokens": { "$ref": "#/$defs/metricAuthority" },
        "output-tokens": { "$ref": "#/$defs/metricAuthority" },
        "cached-input-tokens": { "$ref": "#/$defs/metricAuthority" },
        "total-tokens": { "$ref": "#/$defs/metricAuthority" },
        "cost-usd": { "$ref": "#/$defs/metricAuthority" },
        "first-pass-approval-rate": { "$ref": "#/$defs/metricAuthority" },
        "required-check-pass-rate": { "$ref": "#/$defs/metricAuthority" },
        "conformance-gap-count": { "$ref": "#/$defs/metricAuthority" },
        "escaped-defects": { "$ref": "#/$defs/metricAuthority" }
      }
    },
    "studies": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["id", "label", "method", "groups", "primaryMetric"],
        "properties": {
          "id": { "$ref": "#/$defs/id" },
          "label": { "type": "string", "minLength": 1 },
          "enabled": { "type": "boolean" },
          "unit": { "const": "story" },
          "method": { "enum": ["matched-observational", "phased-rollout", "before-after"] },
          "eligibility": {
            "type": "object",
            "additionalProperties": false,
            "properties": {
              "workTypes": { "type": "array", "uniqueItems": true, "items": { "type": "string", "minLength": 1 } },
              "capabilities": { "type": "array", "uniqueItems": true, "items": { "$ref": "#/$defs/id" } }
            }
          },
          "groups": { "type": "array", "minItems": 2, "maxItems": 2, "items": { "$ref": "#/$defs/group" } },
          "matching": {
            "type": "object",
            "additionalProperties": false,
            "properties": {
              "dimensions": {
                "type": "array",
                "uniqueItems": true,
                "items": { "enum": ["capability", "repository-class", "work-type", "complexity", "risk", "time-period"] }
              },
              "timePeriod": { "enum": ["month", "quarter", "half-year", "year"] },
              "seed": { "type": "string" },
              "weighting": { "enum": ["equal-stratum", "minimum-cohort-count"] }
            }
          },
          "primaryMetric": { "$ref": "#/$defs/primaryMetric" },
          "guardrails": { "type": "array", "items": { "$ref": "#/$defs/guardrail" } },
          "rollout": {
            "type": "object",
            "additionalProperties": false,
            "required": ["declaredAt", "waves", "prePeriodDays", "postPeriodDays", "crossover", "minimumAdherencePercent"],
            "properties": {
              "declaredAt": { "type": "string", "format": "date-time" },
              "waves": {
                "type": "array",
                "minItems": 2,
                "items": {
                  "type": "object",
                  "additionalProperties": false,
                  "required": ["id", "activatedAt"],
                  "properties": {
                    "id": { "$ref": "#/$defs/id" },
                    "activatedAt": { "type": "string", "format": "date-time" },
                    "capabilities": { "type": "array", "uniqueItems": true, "items": { "$ref": "#/$defs/id" } }
                  }
                }
              },
              "prePeriodDays": { "type": "integer", "minimum": 1 },
              "postPeriodDays": { "type": "integer", "minimum": 1 },
              "crossover": { "enum": ["exclude", "as-treated", "intention-to-treat"] },
              "minimumAdherencePercent": { "type": "number", "minimum": 0, "maximum": 100 },
              "requireConcurrentControl": { "type": "boolean" },
              "requirePreTrendCheck": { "type": "boolean" }
            }
          },
          "reporting": {
            "type": "object",
            "additionalProperties": false,
            "properties": {
              "bootstrapSamples": { "type": "integer", "minimum": 100, "maximum": 100000 },
              "confidenceLevel": { "type": "number", "exclusiveMinimum": 0.5, "exclusiveMaximum": 1 }
            }
          },
          "privacy": {
            "type": "object",
            "additionalProperties": false,
            "properties": {
              "individualReporting": { "const": false },
              "minimumCohortSize": { "type": "integer", "minimum": 2 },
              "pseudonymizeContributors": { "type": "boolean" },
              "allowedDimensions": {
                "type": "array",
                "uniqueItems": true,
                "items": { "enum": ["capability", "repository-class", "work-type", "complexity", "risk", "time-period"] }
              }
            }
          }
        }
      }
    }
  }
}
