# Select a workspace

A workspace is a local directory plus its governed capabilities and repositories. The CLI workspace registry is the source of truth.

[Open Workspaces](command:singularityFlow.openWorkspaces)
